[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "sap-odata-agent"
version = "2.0.0"
description = "A guarded, read-only SAP OData runtime for Codex"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115,<1.0",
    "pydantic>=2.7,<3.0",
    "uvicorn>=0.30,<1.0",
]

[project.optional-dependencies]
agent = [
    "mcp>=1.0,<2.0",
]
dev = [
    "httpx>=0.28,<1.0",
    "pytest>=8.0,<9.0",
]

[project.scripts]
sap-odata-build-index = "sap_odata_agent.tools.build_dual_source_index:main"
sapclaw-runtime-mcp = "sap_odata_agent.agent_tools.runtime_mcp_server:main"
sapclaw-runtime-e2e = "sap_odata_agent.tools.run_runtime_codex_e2e:main"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
